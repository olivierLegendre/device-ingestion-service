name: CI

on:
  push:
  pull_request:

jobs:
  unit:
    runs-on: ubuntu-latest
    env:
      DEVICE_INGESTION_PERSISTENCE_BACKEND: in_memory
      DEVICE_INGESTION_POSTGRES_AUTO_INIT: "false"

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e '.[dev]'

      - name: Export OpenAPI
        run: python scripts/export_openapi.py

      - name: Lint
        run: ruff check .

      - name: Type check
        run: mypy src

      - name: Unit tests
        run: pytest -m "not postgres_integration"

  postgres_integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: device_ingestion
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d device_ingestion"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    env:
      DEVICE_INGESTION_TEST_POSTGRES_DSN: postgresql://postgres:postgres@localhost:5432/device_ingestion

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e '.[dev]'

      - name: Postgres integration tests
        run: pytest -m postgres_integration
